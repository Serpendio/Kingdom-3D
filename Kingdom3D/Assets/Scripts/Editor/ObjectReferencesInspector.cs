using System.Collections;
using System.Collections.Generic;
using UnityEditor;

/*[CustomEditor(typeof(ObjectReferences))]
public class ObjectReferencesInspector : Editor
{
    protected static bool ShowOffsetSettings = true;

    private void Awake()
    {
        
    }

    public override void OnInspectorGUI()
    {
        var offsetProperty = serializedObject.FindProperty("Offset");

        ShowOffsetSettings = EditorGUILayout.Foldout(ShowPositionSettings, "Offset Settings");

        if (ShowOffsetSettings)
        {
            EditorGUILayout.PropertyField(offsetProperty);
        }

        serializedObject.ApplyModifiedProperties();
    }
}*/
