using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Scaffolding : MonoBehaviour
{
    const float maxPanelSize = 2;

    private void Awake()
    {
        // setup dimensions (& seed?)

        // could define a list of points for the scaffolding to connect to?
    }

    public void UpdateScaffolding(float percent)
    {
        // 
    }
}
